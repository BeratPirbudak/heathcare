import React from 'react'

type DataTableTypes = {
  columns: any;
  data: any;
}

const DataTable = ({columns, data}: DataTableTypes) => {
  return (
    <div>DataTable</div>
  )
}

export default DataTable